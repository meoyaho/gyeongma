{
  "name": "run-your-name",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-path=server --watch-path=shared server/index.js",
    "build": "vite build",
    "start": "node server/index.js --production",
    "test": "node --test tests/*.test.js",
    "test:browser": "node scripts/check-browser.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "three": "^0.180.0",
    "vite": "^6.1.0",
    "ws": "^8.18.0"
  },
  "overrides": { "qs": "^6.16.0" },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
